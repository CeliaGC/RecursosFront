/* Primero necesitamos tomar la 
etiqueta h1 usando el método querySelector(). */
/* Для начала нам нужно взять h1 с помощью метода querySelector(). */

/* const h1 = document.querySelector("h1"); */

/* Después usamos h1.style.color para 
cambiar el texto h1 de negro a azul. */
/* Затем мы используем h1.style.color для Измените текст H1 с черного на синий. */

const h1 = document.querySelector("h1");
h1.style.color = "blue";

/* Puedes usar esta propiedad style para cambiar un número de 
inline styles de CSS incluyendo background-color, 
border-style, font-size y más. */

/* Вы можете использовать это свойство стиля для изменения количества
Встроенные стили CSS, включая цвет фона,
стиль границы, размер шрифта и многое другое. */