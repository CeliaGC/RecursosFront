/* Si deseamos encontrar en la consola e imprimir el elemento h1 , 
entonces podemos usar ese nombre de etiqueta dentro del querySelector(). */
/* Если мы хотим найти в консоли и вывести элемент h1, тогда мы можем 
использовать это имя тега внутри querySelector(). */

/*const h1Element = document.querySelector("h1");
console.log(h1Element);*/

/* Si quisiera apuntar a  class="list" para imprimir la lista no ordenada 
en la consola, entonces usaría  .list dentro del  querySelector(). */
/* Если я хочу указать на class="list", чтобы распечатать неупорядоченный
список в консоли вы должны использовать.list внутри запроса Selector(). */

const listTV = document.querySelector(".list");
console.log(listTV);

/* El . antes de list  le dice a la computadora que
apunte  al nombre de la Clase. Si deseas apuntar a un id entonces 
te recomiendo usar el símbolo # antes del nombre. */
/*Тем. Before List сообщает компьютеру, что наведите указатель
мыши на имя класса. Если вы хотите указать 
на идентификатор, то Я рекомендую использовать символ # перед именем. */

/* clg (para console.log)
console.log(first) */